pipeline {
    agent any
    stages {
        stage('API Compile') {
            steps {
                script {
                    withMaven(maven: 'maven_3_9_6') {
                        sh 'mvn clean compile'
                    }
                }
            }
        }

    }
}
